import styled from 'styled-components';
import { CompareArrows } from 'styled-icons/material/CompareArrows';

export const CompareButtonIcon = styled(CompareArrows)`
  color: ${props => props.theme.black};
  width: 22px;
  height: 22px;
`;
